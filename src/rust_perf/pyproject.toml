[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "rokbot_perf"
version = "0.1.0"
description = "Rust performance extensions for ROK Bot Engine"
requires-python = ">=3.11"
