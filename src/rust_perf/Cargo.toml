[package]
name = "rokbot_perf"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.20", features = ["extension-module"] }
image = "0.24"

[profile.release]
lto = true
opt-level = 3
